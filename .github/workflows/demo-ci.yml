name: Demo CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: echo "Building..."

      - name: Test step
        run: echo "Running tests..."

      - name: Deploy step
        run: echo "Deploying..."
